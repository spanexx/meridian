# MERIDIAN — End-of-Session Handoff (2026-08-18, ~19:30)

## Mode
Operating mode is **AUTO** (`state` file = `auto`). The user flipped it in a
prior session; only they can switch it back. Honor AUTO: the agent may decide
HOW, never WHAT — follow `docs/`, `CONTEXT.md`, `decisions.md`. Document every
AUTO decision in `sessions/decisions.md`.

## Branch
`feat/frontend-data-layer` (local, unpushed). master at `2e0a9c8`.

## What shipped THIS session (all committed)
- `4660ba1` feat(frontend): rewire payouts page to injected ApiClient (Step 6)
  — `payouts.data.ts` deleted; page injects `apiClient.payoutsList()`.
- `9ce7c48` feat(frontend): wire ApiClient via API_TRANSPORT token + fix
  latent seed bugs (Step 7) — transport seam (MockTransport dev / HttpTransport
  prod) chosen only in `app.config.ts`.
- `e27eebb` fix(frontend): pin sidebar bottom action row on short screens
  (cline-one job 6) — `.sidebar` no longer scrolls; `.sidebar-scroll` does;
  bottom action row is pinned `.sidebar-footer`.

## Verification state (green)
`tsc --noEmit` clean · vitest 956/956 · Playwright e2e 9/9 (payouts,
byte-identical). All 10 pre-commit hooks pass.

## Backend-readiness audit (cline-one job 7) — VERDICT: NOT READY
The transport seam is a correct single switch point (`app.config.ts` only
picks Mock vs Http; flip `useMock`). BUT only `payouts.page.ts` injects
`ApiClient`. ~14 other pages render hardcoded wireframe fixtures (confirmed by
grep: only payouts.page.ts + core/api + app.config reference ApiClient). 5
`ApiClient` methods have NO backend contract: `/executions`,
`/executions/{id}`, `/payouts`, `/members/me/payouts`, `/notifications`. Auth
token is a stub `() => null`. Full report:
`.cline-one-reports/20260818-backend-readiness.md` (gitignored).

## Uncommitted working-tree state (decide next session)
- `frontend/src/app/core/api/api-client.spec.ts` — 95 insertions of NEW test
  coverage (memberSettings, login2fa, opportunitiesMine, vettingQueue,
  executions, payoutsMine, communities, governance, etc.). NOT authored this
  session — likely a prior in-flight expansion. Decide: commit as its own
  commit, or fold into the data-layer PR.
- `frontend/e2e/screenshots/*.png` — refreshed by e2e runs; gitignored? no.
  Safe to leave or commit.
- `sessions/.handoff-log.md` — handoff breadcrumb (auto).

## Dev server (session-scoped — will NOT exist next session)
A resident `ng serve --port 8999 --host 0.0.0.0` is running (proc_3a3f25c5e0d0)
for the user's phone check. Per the no-daemons rule it should be killed when
the session ends. Re-run `cd frontend && npx ng serve --port 8999 --host
0.0.0.0` if needed.

## Next agent: what to pick up
1. User's stated goal: frontend must be backend-switch-ready (flip one config =
   done). That is NOT met — needs a pack rewiring ~14 pages to ApiClient + 5
   API contracts in `docs/apis/`. That is a big multi-session chunk → user must
   trigger `wayfinder` or approve a plan first (plan-first rule).
2. Pending decision: PR the data-layer branch to master, or keep stacking?
3. Read `.cline-one-reports/20260818-backend-readiness.md` for the full gap
   table before planning.

## Key files
- `frontend/src/app/app.config.ts` (transport seam — the one switch)
- `frontend/src/app/core/api/*` (ApiClient, transports, mock seed)
- `docs/frontend/01-frontend-backlog.md` (pack order A–E)
- `sessions/decisions.md`, `sessions/.handoff-log.md`
